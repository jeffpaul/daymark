{
  "name": "daymark",
  "version": "0.7.0",
  "description": "Personal Site Publisher Mode for WordPress. Dev dependencies for browser E2E tests and the block editor build only.",
  "private": true,
  "license": "GPL-2.0-or-later",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jeffpaul/daymark.git"
  },
  "scripts": {
    "build": "wp-scripts build",
    "start": "wp-scripts start",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@wordpress/scripts": "^34.0.0"
  }
}
